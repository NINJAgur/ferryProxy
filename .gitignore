# Python
server/.venv/
server/__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/

# Env — never commit real keys
.env
server/.env

# Locally generated CA bundle (machine-specific: public roots + local TLS proxy cert)
server/.certs/

# Entitlement store — stands in for a database, holds purchase receipts
server/.entitlements.json

# Node / Expo
client/node_modules/
client/.expo/
client/dist/
client/npm-debug.*

# OS
.DS_Store
Thumbs.db

# Personal setup notes, not part of the project
BILLING-SETUP.md
