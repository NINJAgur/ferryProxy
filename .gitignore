# Python
server/.venv/
server/__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/

# Env — never commit real keys
.env
server/.env

# Locally generated CA bundle (machine-specific: public roots + local TLS proxy cert)
server/.certs/

# Account/entitlement store — stands in for a database, holds user emails
server/.accounts.json

# Node / Expo
client/node_modules/
client/.expo/
client/dist/
client/npm-debug.*

# OS
.DS_Store
Thumbs.db
